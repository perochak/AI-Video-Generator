fastapi==0.110.1
uvicorn[standard]==0.29.0
TTS==0.22.0
diffusers==0.27.2
transformers==4.39.3
ftplib  # This is part of the Python standard library, so you can remove it.
moviepy==1.0.3
torch==2.2.1  # Make sure this matches your GPU environment (CUDA 11.8 or similar)
